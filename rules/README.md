# rules/

放编码规范、提示片段、跨 skill 共享的指南。

每个 `.md` 自由命名，按主题分类（例如 `vue3-guidelines.md`、`typescript-coding-style.md`）。

在 skill / agent / command / CLAUDE.md 里通过相对路径引用：

```
参见 [Vue3 规范](../rules/vue3-guidelines.md)
```
